reset;

# MINOS does not do MIP (no integers)
# but CPLEX does not do NLP (there is a square root in the model).
option solver minos;

model min_area.mod;

data min_area.dat;

solve;

display component_counts;
